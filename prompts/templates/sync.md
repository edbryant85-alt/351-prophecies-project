# FS3 Sync Pass: ${page_id}

You are working on `${page_id}` at the `${current_stage}` stage.

Target files:
${file_list}

Current workflow metadata:
- Type: `${page_type}`
- Status: `${current_status}`
- Confidence: `${current_confidence}`
- Dependencies: `${dependencies}`

Known weak spots:
${known_weak_spots}

Preview and index context:
${preview_links}

Sync-stage goals:
- Align overview links, related-claims lists, tracker notes, and metadata with the current file set.
- Keep preview references and repo paths accurate after structural edits.
- Avoid body rewrites unless a small metadata or navigation fix is required.
- Leave the repo in a coherent state for the next editorial pass.

Project constraints:
${style_constraints}
