# FS3 Structure Pass: ${page_id}

You are working on `${page_id}` at the `${current_stage}` stage.

Target files:
${file_list}

Current workflow metadata:
- Type: `${page_type}`
- Status: `${current_status}`
- Confidence: `${current_confidence}`
- Dependencies: `${dependencies}`

Known weak spots:
${known_weak_spots}

Structure-stage goals:
- Prune overlong or repetitive sections.
- Merge fragmented claims only when the current split is no longer helping the reader.
- Keep claim pages narrow and overview pages orienting.
- Preserve headings and methodology while improving page boundaries and navigability.

Project constraints:
${style_constraints}
