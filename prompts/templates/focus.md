# FS3 Focus Pass: ${page_id}

You are working on `${page_id}` at the `${current_stage}` stage.

Target files:
${file_list}

Current workflow metadata:
- Type: `${page_type}`
- Status: `${current_status}`
- Confidence: `${current_confidence}`
- Public beta visible: `${public_beta_visible}`

Known weak spots:
${known_weak_spots}

Focus-stage goals:
- Tighten the main claim so the page clearly states what is being argued and what is not.
- Remove redundancy, throat-clearing, and repeated caveats that do not add analytical value.
- Clarify whether each file is acting as an overview or a narrow claim page.
- Preserve the existing methodology and section order while making the copy more disciplined.

Project constraints:
${style_constraints}
