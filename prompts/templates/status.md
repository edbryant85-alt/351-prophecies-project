# FS3 Status Pass: ${page_id}

You are working on `${page_id}` at the `${current_stage}` stage.

Target files:
${file_list}

Current workflow metadata:
- Type: `${page_type}`
- Status: `${current_status}`
- Confidence: `${current_confidence}`

Known weak spots:
${known_weak_spots}

Status-stage goals:
- Decide whether the page or cluster currently fits `Flagship Analysis`, `Reviewed Analysis`, or `Draft Analysis`.
- Decide whether the confidence label should be `High`, `Moderate`, or `Low`.
- Keep the labels honest to the actual editorial state.
- Update only status-related metadata and any tiny explanatory note needed for consistency.

Project constraints:
${style_constraints}
