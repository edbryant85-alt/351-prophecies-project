# FS3 Steelman Pass: ${page_id}

You are working on `${page_id}` at the `${current_stage}` stage.

Target files:
${file_list}

Current workflow metadata:
- Type: `${page_type}`
- Status: `${current_status}`
- Confidence: `${current_confidence}`

Known weak spots:
${known_weak_spots}

Steelman-stage goals:
- Strengthen the best Christian interpretation without overstating certainty.
- Strengthen the best skeptical and historical-critical objections fairly.
- Strengthen the main Jewish interpretive options where relevant.
- Make disagreements legible instead of collapsing them into one generic “pro/con” frame.

Project constraints:
${style_constraints}
