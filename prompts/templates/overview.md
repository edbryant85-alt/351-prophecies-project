# FS3 Overview Reconciliation Pass: ${page_id}

You are working on `${page_id}` at the `${current_stage}` stage.

Target files:
${file_list}

Current workflow metadata:
- Type: `${page_type}`
- Status: `${current_status}`
- Confidence: `${current_confidence}`

Known weak spots:
${known_weak_spots}

Overview-stage goals:
- Reconcile the overview page with the surviving claim pages after recent structure changes.
- Make sure the overview summarizes the cluster rather than duplicating claim-page detail.
- Keep the “Related Prophecy Claims” section accurate and trimmed to the current cluster.
- Preserve the existing methodology and section order.

Project constraints:
${style_constraints}
