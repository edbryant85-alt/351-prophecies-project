# FS3 Validation Pass: ${page_id}

You are working on `${page_id}` at the `${current_stage}` stage.

Target files:
${file_list}

Current workflow metadata:
- Type: `${page_type}`
- Status: `${current_status}`
- Confidence: `${current_confidence}`

Known weak spots:
${known_weak_spots}

Validation-stage goals:
- Remove lingering placeholder or prototype language.
- Check markdown links, preview links, and related-claims links for staleness.
- Confirm metadata, status labels, and notes are still correct after the latest edits.
- Do not expand the scope; treat this as a final sanity pass before completion.

Project constraints:
${style_constraints}
